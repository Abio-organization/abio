# Leave unset (or "/api/v1") for local dev — Vite's dev server proxies it
# to localhost:9800, so requests stay same-origin (no CORS preflight).
# Set an absolute URL for production, e.g. https://api.abio.site/api/v1
VITE_API_BASE_URL=/api/v1
